import React from 'react'
import './OutlineBtn.css'

const OutlineBtn = ({title}) => {
    return (
        <>
            <button className='outline-btn'>{title}</button>
        </>
    )
}

export default OutlineBtn
